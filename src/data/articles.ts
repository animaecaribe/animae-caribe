export type Article = {
  title: string;
  date: string;
  category: string;
  excerpt: string;
};

// News is supplied by the published news and media archive.
export const articles: Article[] = [];
